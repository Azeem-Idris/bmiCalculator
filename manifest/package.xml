<?xml version="1.0" encoding="UTF-8"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- List only the metadata you intend to deploy. 
         Wildcards are NOT allowed for CustomObject; list each object explicitly or remove the block. -->

    <!-- Remove this block if you are not deploying custom objects right now. -->
    <types>
        <members>MyCustomObject__c</members>
        <members>AnotherObject__c</members>
        <name>CustomObject</name>
    </types>

    <!-- Deploy the LWC bundle(s). LightningComponentBundle supports wildcard, 
         but we list bmiCalculator explicitly to be precise. -->
    <types>
        <members>bmiCalculator</members>
        <name>LightningComponentBundle</name>
    </types>

    <version>57.0</version>
</Package>
